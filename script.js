(function(){const carousel=document.querySelector('[data-carousel]');const previous=document.querySelector('[data-prev]');const next=document.querySelector('[data-next]');const topButton=document.querySelector('[data-top]');function slide(direction){if(!carousel)return;const card=carousel.querySelector('.page-preview');const distance=card?card.getBoundingClientRect().width+20:320;carousel.scrollBy({left:direction*distance,behavior:'smooth'})}previous?.addEventListener('click',()=>slide(-1));next?.addEventListener('click',()=>slide(1));carousel?.addEventListener('keydown',event=>{if(event.key==='ArrowLeft')slide(-1);if(event.key==='ArrowRight')slide(1)});document.querySelectorAll('[data-buy]').forEach(button=>{button.addEventListener('click',()=>{const note=button.parentElement?.querySelector('.checkout-note');if(note)note.hidden=false})});function updateTopButton(){topButton?.classList.toggle('is-visible',window.scrollY>700)}window.addEventListener('scroll',updateTopButton,{passive:true});topButton?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));updateTopButton()})();
